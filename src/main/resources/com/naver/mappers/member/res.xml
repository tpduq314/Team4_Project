<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Res">

	 <insert id="res_in">
		insert into res (res_code, res_count01, res_count02, res_count03,
		res_total_price,res_date, mem_id)
		values(#{res_code},#{res_count01},#{res_count02},
		#{res_count03},#{res_total_price},sysdate,#{mem_id})
	</insert>

</mapper>
 